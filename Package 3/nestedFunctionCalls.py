#Simple program using nested fucntion calls.
#author: danielCodingGuy

print(round(abs(float(input("Enter a whole positive number: "))))) #We can use a lot less lines of code using nested function calls.

# Without nested function calls it would look like this:
# num = input("Enter a whole positive number: ")
# num = float(num)
# num = abs(num)
# num = round(num)
# print(num)